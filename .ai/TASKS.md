# TASKS

Use this board to coordinate manual handoff between planner, implementer, and reviewer.

Status values:
- `in_planning`
- `ready_for_implement`
- `in_implementation`
- `ready_for_review`
- `in_review`
- `ready_to_commit`
- `changes_requested`
- `done`

Command expectations:
- planner moves tasks into `in_planning` and `ready_for_implement`
- implementer moves tasks into `in_implementation`, `ready_for_review`, and `done`, and resumes work from `changes_requested` and `ready_to_commit`
- reviewer moves tasks into `in_review`, `ready_to_commit`, or `changes_requested`
- `status_cycle` should report deterministic task status, current owner role, and next recommended action based on this board

| Task ID | Scope | Status | Acceptance Criteria | Evidence | Next Role |
| --- | --- | --- | --- | --- | --- |
| T-001 | Add `profile` field to `.ai/config.json` parsing (`internal/mcp/config.go`), default `"full"`, validate against `{"full","lite"}`, expose `ActiveProfile()` helper, and include `"profile": "full"` in the scaffold template `internal/template/templates/base/ai/config.json.tmpl`. | done | Missing field loads as `"full"`; `"lite"` accepted; unknown value rejected with a clear error; existing 0.9.x fixtures still parse; scaffolded `.ai/config.json` contains the field; new `internal/mcp/config_test.go` cases cover all four paths; `internal/template/engine_test.go` and `internal/scaffold/scaffold_test.go` assertions updated; `go test ./...` green. | `go fmt ./...`; `go vet ./...`; `go test ./...` | none |
| T-002 | New `aide profile` parent command in `cmd/profile.go` with `full` and `lite` subcommands; no-arg form prints active profile and config path; subcommands persist the new profile via a `WriteProfile` helper in `internal/mcp/config.go` using map-based read-modify-write; `--help` on subcommands prints mode-detail reference without writing; mid-cycle switch shows a one-line advisory. | done | `aide profile` prints current profile + path; `aide profile full` and `aide profile lite` persist correctly and survive round-trips; `aide profile <mode> --help` does not modify `.ai/config.json`; advisory appears when tasks are mid-flight; unknown values rejected; new `cmd/profile_test.go` covers each path; `go test ./...` green. | `go fmt ./...`; `go vet ./...`; `go test ./...` | none |
| T-003 | New `aide dev` launcher in `cmd/dev.go` mirroring `cmd/implement.go` patterns; new `internal/template/templates/base/ai/prompts/dev.md.tmpl` containing the full dev session prompt (hat-switching loop, 3-attempt cap, mechanical-vs-semantic rules, `next_task`/`all_task`/`rework_task`/`commit_task`/`status_cycle` semantics, halt-block format, verbatim test-weakening guardrail, real-commit-per-task policy); `aide dev` refuses in full mode with a helpful message. | done | `aide dev` in full mode prints the refusal naming `aide implement`/`aide review` and exits non-zero; `aide dev` in lite mode calls `runRoleLaunch("dev", "dev.md", "codex", args)`; new `cmd/dev_test.go` covers both paths; `dev.md.tmpl` contains the verbatim test-weakening guardrail and the documented halt block; `internal/template/engine_test.go` and `internal/scaffold/scaffold_test.go` assert key dev.md strings; `go test ./...` green. | `go fmt ./...`; `go vet ./...`; `go test ./...` | none |
| T-004 | Profile-aware refusals in lite mode for `cmd/implement.go`, `cmd/review.go`, `cmd/po.go`. Each pre-checks the active profile via `loadLaunchConfig` and returns an error message naming the correct alternative; full-mode behavior unchanged. | done | In lite mode, `aide implement`, `aide review`, and `aide po` each print their specific refusal message and exit non-zero; in full mode all three behave exactly as before; new tests in `cmd/implement_test.go`, `cmd/review_test.go`, `cmd/po_test.go` cover both branches per command; `go test ./...` green. | `go fmt ./...`; `go vet ./...`; `go test ./...` | none |
| T-005 | `aide init` `--profile lite\|full` flag plus a wizard prompt step in `internal/wizard`; `internal/scaffold` accepts a `Profile` option, always writes `dev.md` and `implementer.md`/`reviewer.md` (so mid-cycle switching is frictionless), and `internal/scaffold/summary.go` lists profile-appropriate next commands. | done | `aide init --profile lite foo` and `aide init --profile full foo` both succeed and write the chosen profile into `.ai/config.json`; the interactive wizard offers the choice with `full` default; scaffolded projects contain all three prompt files (planner, dev, implementer/reviewer); post-init summary lists `aide plan` + `aide dev` for lite and `aide plan` + `aide implement` + `aide review` + `aide po` for full; updated `internal/scaffold/summary_test.go`, `internal/scaffold/scaffold_test.go`, and `internal/wizard` tests pass; `go test ./...` green. | `go fmt ./...`; `go vet ./...`; `go test ./...` | none |
| T-006 | CLI help overhaul: every command in `cmd/` (`init`, `cycle` and subcommands, `plan`, `implement`, `review`, `po`, `pr`, `update`, `mcp` and subcommands, `dev`, `profile` and subcommands) gains a meaningful `Long` and at least one `Example`; root `aide` gets a real overview `Long` covering both modes and `aide profile --help`. | done | Every registered Cobra command exposes a non-empty `Long` and at least one `Example` entry; a table-driven test in `cmd/root_test.go` enforces this for all subcommands and is green; the root command's `Long` mentions both modes and the recommended entry path; `go test ./...` green. | `go fmt ./...`; `go vet ./...`; `go test ./...` | none |
| T-007 | Documentation pass: `README.md` (project root) gains a "Modes" section with comparison table and "How to switch" subsection; project-root `AGENTS.md` documents the lite profile end-to-end including the verbatim test-weakening guardrail; `internal/template/templates/base/AGENTS.md.tmpl` and `internal/template/templates/base/README.md.tmpl` mirror those updates; scaffold/template tests updated to assert the new strings for both profiles. | done | `README.md` contains the Modes section and table; `AGENTS.md` contains the lite-profile docs and the verbatim guardrail; template files mirror those updates so a fresh scaffold ships with the same content; `internal/template/engine_test.go` and `internal/scaffold/scaffold_test.go` assertions match the new content and pass; `go test ./...` green. | `go fmt ./...`; `go vet ./...`; `go test ./...` | none |
